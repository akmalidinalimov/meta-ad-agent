# Development / test dependencies (not required to run the app).
# Install with: pip install -r backend/requirements-dev.txt
-r requirements.txt

pytest==9.0.3
# NOTE: async code is currently tested via asyncio.run() inside sync test
# functions, so pytest-asyncio is intentionally NOT required yet. Add it here
# if/when async-def test functions are introduced.
